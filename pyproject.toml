
[tool.ty.environment]
python = ".venv"

[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "knifflex"
version = "0.0.1"

[tool.setuptools]
packages = ["knifflex"]
